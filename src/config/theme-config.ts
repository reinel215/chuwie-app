import { DefaultTheme as PaperDefaultTheme } from "react-native-paper";

export const theme = {
  ...PaperDefaultTheme,
  colors: {
    ...PaperDefaultTheme.colors,
    primary: "#097e5c",
    secondary: "#aec704",
    textPrimary: "#ffffff"
  },
};